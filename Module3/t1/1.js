var buttonElement = window.document.querySelector('.button');

    // Add a click event listener to the selected button
buttonElement.addEventListener('click', function() {
    alert('Button Clicked!');
});